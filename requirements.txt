blinker==1.8.2
click==8.1.7
Flask==3.0.3              # Keep only if required for web serving
Flask-Cors==5.0.0         # Keep only if CORS is essential
itsdangerous==2.2.0
Jinja2==3.1.4
joblib==1.4.2
MarkupSafe==2.1.5
numpy==1.21.0             # Slimmer version, test compatibility
scikit-learn==0.24.0      # Slimmer version
scipy==1.7.0              # Lighter version, test compatibility
threadpoolctl==3.5.0
Werkzeug==2.2.3           # Downgrade for smaller version size
